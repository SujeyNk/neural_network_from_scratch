# Core packages used by the NumPy neural-network notebook
numpy>=1.24,<3.0
scikit-learn>=1.2,<2.0
matplotlib>=3.7,<4.0
jupyter>=1.0,<2.0
ipykernel>=6.0,<7.0

# Used only by utils_train.py, if that optional PyTorch helper is retained
torch>=2.0,<3.0
tqdm>=4.65,<5.0
